<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - DMS</title>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" />

    <link rel="stylesheet"
          href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css"
          integrity="sha512-6T6M7wOQDyOIoHhOHEaRy1Ee1KZ8mPyb+fTkXIzX1Ac2iL3+OQMnzZbXdbP/VDwglFdtSEzPG8Nf57z4j7Y6sw=="
          crossorigin="anonymous" referrerpolicy="no-referrer" />
    <link href="/vendor/fontawesome-free/css/all.min.css" rel="stylesheet" type="text/css">


    <style>
        html, body {
            height: 100%;
        }

        body {
            /* FIX: Change display to flex-direction: column for proper stacking */
            display: flex;
            flex-direction: column;
            /* FIX: Center items horizontally */
            align-items: center;
            /* FIX: Align content to the start (top) if the page scrolls, or center if short */
            justify-content: center;
            background-color: #f8f9fa;
        }

        .auth-container {
            /* The container takes up space, pushes the footer down */
            width: 100%;
            max-width: 420px;
            padding: 20px 25px 10px;
            background: white;
            border-radius: 15px;
            box-shadow: 0 0 15px rgba(0, 0, 0, 0.1);
            /* Add margin to ensure there's space above and below,
                   especially if you remove justify-content: center above */
            margin-top: 100px;
            margin-bottom: 20px; /* IMPORTANT: Ensures space above the footer */
        }

        footer {
            /* FIX: Remove position: absolute to place it in the normal flow */
            position: relative; /* Use relative or remove the property */
            /* Position it at the bottom of the visible screen area */
            bottom: 0;
            width: 100%;
            text-align: center;
            color: #777;
            /* Add a small amount of padding/margin if needed */
            padding: 10px 0;
            /* Ensure it's centered with the rest of the body flex */
            max-width: 420px; /* Optional: Keep it aligned with the container max-width */
            margin: 0 auto;
        }
    </style>
</head>

<body>
    <main class="auth-container">
        @RenderBody()
    </main>
    <footer>
        <p>&copy; 2025 - DMS</p>
    </footer>


    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>